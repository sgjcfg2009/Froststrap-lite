<UserControl x:Class="Bloxstrap.UI.Elements.Controls.SquareCard" 
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" 
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" 
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml" 
             mc:Ignorable="d" SnapsToDevicePixels="True" x:Name="Control">

    <ui:Card SnapsToDevicePixels="True" Padding="14" Margin="0,6,6,6">
        <Grid MinHeight="140">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            
            <TextBlock Grid.Row="0" FontSize="18" TextWrapping="Wrap"
                       Text="{Binding Header, ElementName=Control}">
                <TextBlock.Style>
                    <Style TargetType="TextBlock">
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding Header, ElementName=Control}" Value="{x:Null}">
                                <Setter Property="Visibility" Value="Collapsed"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </TextBlock.Style>
            </TextBlock>

            
            <TextBlock Grid.Row="1" FontSize="14" TextWrapping="Wrap" TextTrimming="CharacterEllipsis"
                       Margin="0,6,0,0" Foreground="{DynamicResource TextFillColorTertiaryBrush}"
                       Text="{Binding Description, ElementName=Control}">
                <TextBlock.Style>
                    <Style TargetType="TextBlock">
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding Description, ElementName=Control}" Value="{x:Null}">
                                <Setter Property="Visibility" Value="Collapsed"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </TextBlock.Style>
            </TextBlock>

            <ContentPresenter Grid.Row="2" Content="{Binding InnerContent, ElementName=Control}"
                              VerticalAlignment="Bottom" HorizontalAlignment="Stretch" Margin="0,8,0,0">
                <ContentPresenter.Style>
                    <Style TargetType="ContentPresenter">
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding InnerContent, ElementName=Control}" Value="{x:Null}">
                                <Setter Property="Visibility" Value="Collapsed"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </ContentPresenter.Style>
            </ContentPresenter>
        </Grid>
    </ui:Card>
</UserControl>